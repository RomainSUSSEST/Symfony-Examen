{% extends 'base.html.twig' %}

{% block title %}Ajouter Contrat{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
        .example-wrapper input { margin: 10px; }
        button[type="submit"] { border: 2px solid black; width: 120px; height: 30px; background-color: black; color: #FFF; }
    </style>

    <div class="example-wrapper">
        <h1>Ajouter un contrat</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
